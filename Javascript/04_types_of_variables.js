// there are many types of variable
//  in js and write as start with let 
// Premetive data types are pre-defined
let name = "Raza" // Now the name is saved as a string as its inside ""
let age = 12; //its an int number
let height = 5.4; //its a float number
let city; //its an undefined data type
let books = null; //its also a data type
console.log(typeof(name))
