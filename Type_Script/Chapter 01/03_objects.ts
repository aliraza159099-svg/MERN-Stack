//just like in js here we use the key value pais objects with its rules

let student:{name: string , age: number}={
    name:"Raza",
    age:19
}
//we can update the data but in same type
student.age = 20;
console.log(student)